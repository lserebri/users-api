import {Router} from 'express'

export const usersRouter = Router();

usersRouter.post('/add-user-account', async (req, res) => {
    try {

    }
    catch (error) {
        console.error(error);
    }
})
